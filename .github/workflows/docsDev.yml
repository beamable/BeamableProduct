name: Docs CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'client/**'
      - 'microservice/**'

jobs:
  doxygen:
    uses: Beamable/BeamableProduct/.github/workflows/docsCommon.yml@main
    with:
      s3Dest: DEV
      