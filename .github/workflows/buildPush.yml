name: Push Build

on:
  push:
    branches:
      - 'bug/beam-2619'

jobs:
  ios:
    name: Build iOS .ipa and upload to Testflight
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - name: Check Directories
        run: ls /Users/runner/work/BeamableProduct/BeamableProduct/build/ios/BeamableProductDev.mobileprovision  && ls /Users/runner/work/BeamableProduct/build/ios/BeamableProductDev.mobileprovision
         