# Beamable Templates

This folder contains project templates used by the Beamable CLI (`beam project new`). These files are not intended to be used directly. Use the CLI to create new microservice/storage projects which will scaffold from these templates.

## For maintainers
If you need to modify a template for development reasons, update files here and then use the CLI to generate a project to validate changes.
