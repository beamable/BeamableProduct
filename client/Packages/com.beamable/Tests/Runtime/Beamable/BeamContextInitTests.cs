using Beamable;
using Beamable.Api.Payments;
using Beamable.Common;
using Beamable.Common.Api;
using Beamable.Common.Api.Auth;
using Beamable.Common.Api.Mail;
using Beamable.Common.Api.Notifications;
using Beamable.Common.Api.Presence;
using Beamable.Common.Api.Social;
using Beamable.Common.Dependencies;
using Beamable.Platform.Tests;
using Beamable.Player;
#if BEAMABLE_PURCHASING
using Beamable.Purchasing;
#endif
using Beamable.Tests.Runtime;
using NUnit.Framework;
using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.TestTools;
using MailSearchResponse = Beamable.Api.Autogenerated.Models.MailSearchResponse;

namespace Tests.Runtime.Beamable
{
	public class BeamContextInitTests : BeamContextTest
	{
		[UnityTest]
		public IEnumerator SuccessfullInit()
		{
			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			Assert.IsTrue(Context.OnReady.IsCompleted);
			Assert.AreEqual(PromiseBase.Unit, Context.OnReady.GetResult());
		}

		[UnityTest]
		public IEnumerator ErrorOutAfterAttempts()
		{
			PromiseBase.SetPotentialUncaughtErrorHandler((promise, err) => { });
			LogAssert.ignoreFailingMessages = true;

			MockPlatformRoute<User> failingMockRequest = null;
			var config = ScriptableObject.CreateInstance<CoreConfiguration>();
			config.ContextRetryDelays = new double[] { .1, .1, .1 };
			config.EnableInfiniteContextRetries = false;

			TriggerContextInit(builder =>
			{
				OnRegister(builder);
				// also swap out core config.
				builder.RemoveIfExists<CoreConfiguration>();

				builder.AddSingleton(config);
			}, context =>
			{
				context.Requester.MockRequest<TokenResponse>(Method.POST, "/basic/auth/token")
						 .WithNoAuthHeader()
						 .WithJsonFieldMatch("grant_type", "guest")
						 .WithResponse(new TokenResponse
						 {
							 access_token = MockBeamContext.ACCESS_TOKEN,
							 refresh_token = "test_refresh",
							 expires_in = 10000,
							 token_type = "test_token"
						 });

				context.Requester.MockRequest<TokenResponse>(Method.POST, "/basic/auth/token")
					   .WithNoAuthHeader()
					   .WithJsonFieldMatch("grant_type", "refresh_token")
					   .WithResponse(new TokenResponse { access_token = MockBeamContext.ACCESS_TOKEN, refresh_token = "refresh_test" });

				failingMockRequest = context.Requester.MockRequest<User>(Method.GET, "/basic/accounts/me")
										 .WithResponse(new RequesterException("", "GET", "url", 401, ""))
										 .WithToken(MockBeamContext.ACCESS_TOKEN)

					;

				context.AddPubnubRequests()
					   .AddSessionRequests();
			});

			var errorWasTriggered = false;
			var apiErrorWasTriggered = false;
			Exception exception = null;

			Context.OnReady.Error(err =>
			{
				errorWasTriggered = true;
				exception = err;
			});

			Context.OnReady.Map<Unit>(x => x).Error(err =>
			{
				apiErrorWasTriggered = true;
			});
			yield return Context.OnReady.ToYielder();
			Assert.IsTrue(apiErrorWasTriggered);
			Assert.IsTrue(errorWasTriggered);
			Assert.IsInstanceOf<BeamContextInitException>(exception);
			var beamException = (BeamContextInitException)exception;

			Assert.AreEqual(config.ContextRetryDelays.Length, failingMockRequest.CallCount);
			Assert.AreEqual(config.ContextRetryDelays.Length, beamException.Exceptions.Length);
			for (var i = 0; i < config.ContextRetryDelays.Length; i++)
			{
				Assert.IsNotNull(beamException.Exceptions[i]);
			}
			Assert.AreEqual(Context, beamException.Ctx);

		}

		[UnityTest]
		public IEnumerator InfiniteRetriesDoNotOverflowErrorBuffer()
		{
			PromiseBase.SetPotentialUncaughtErrorHandler((promise, err) => { });
			LogAssert.ignoreFailingMessages = true;

			var sawIndexOutOfRange = false;
			void TrackLog(string condition, string stackTrace, LogType type)
			{
				if ((condition?.Contains(nameof(IndexOutOfRangeException)) ?? false) ||
				    (stackTrace?.Contains(nameof(IndexOutOfRangeException)) ?? false))
				{
					sawIndexOutOfRange = true;
				}
			}

			Application.logMessageReceived += TrackLog;
			try
			{
				MockPlatformRoute<User> failingMockRequest = null;
				var config = ScriptableObject.CreateInstance<CoreConfiguration>();
				config.ContextRetryDelays = new double[] { .01, .01 };
				config.EnableInfiniteContextRetries = true;

				TriggerContextInit(builder =>
				{
					OnRegister(builder);
					builder.RemoveIfExists<CoreConfiguration>();
					builder.AddSingleton(config);
				}, context =>
				{
					context.Requester.MockRequest<TokenResponse>(Method.POST, "/basic/auth/token")
						   .WithNoAuthHeader()
						   .WithJsonFieldMatch("grant_type", "guest")
						   .WithResponse(new TokenResponse
						   {
							   access_token = MockBeamContext.ACCESS_TOKEN,
							   refresh_token = "test_refresh",
							   expires_in = 10000,
							   token_type = "test_token"
						   });

					context.Requester.MockRequest<TokenResponse>(Method.POST, "/basic/auth/token")
						   .WithNoAuthHeader()
						   .WithJsonFieldMatch("grant_type", "refresh_token")
						   .WithResponse(new TokenResponse
						   {
							   access_token = MockBeamContext.ACCESS_TOKEN,
							   refresh_token = "refresh_test"
						   });

					failingMockRequest = context.Requester.MockRequest<User>(Method.GET, "/basic/accounts/me")
											 .WithResponse(new RequesterException("", "GET", "url", 401, ""))
											 .WithToken(MockBeamContext.ACCESS_TOKEN);

					context.AddPubnubRequests()
						   .AddSessionRequests();
				});

				// The old bug appears only after attempts exceed ContextRetryDelays.Length.
				var requiredAttempts = config.ContextRetryDelays.Length + 3;
				var timeout = Time.realtimeSinceStartup + 2f;
				while (failingMockRequest.CallCount < requiredAttempts &&
				       Time.realtimeSinceStartup < timeout)
				{
					yield return null;
				}

				Assert.GreaterOrEqual(failingMockRequest.CallCount, requiredAttempts,
									  "The retry loop must run past the configured retry-delay array length.");
				Assert.IsFalse(sawIndexOutOfRange,
							   "Infinite BeamContext retries must not overflow the fixed-size error buffer.");
				Assert.IsFalse(Context.OnReady.IsCompleted,
							   "A persistently failing infinite retry should keep OnReady pending.");
			}
			finally
			{
				Application.logMessageReceived -= TrackLog;
				LogAssert.ignoreFailingMessages = false;
			}
		}
	}

	public class PlayerSocialMailSuppressionTests : BeamContextTest
	{
		private const string MailUpdateChannel = "MAILBOX.UPDATE";
		private const long OtherPlayerId = 5678;

		private bool _originalSuppressAutomaticMailUpdatesFromPlayerSocial;
		private MockPlatformRoute<MailSearchResponse> _mailSearchRoute;
		private MockPlatformRoute<EmptyResponse> _sendFriendInviteRoute;

		[SetUp]
		public void SaveConfiguration()
		{
			_originalSuppressAutomaticMailUpdatesFromPlayerSocial =
				CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial;
		}

		[TearDown]
		public void RestoreConfiguration()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial =
				_originalSuppressAutomaticMailUpdatesFromPlayerSocial;
		}

		protected override void OnInit(MockBeamContext ctx)
		{
			base.OnInit(ctx);

			ctx.Requester.MockRequest<SocialList>(Method.GET, "/basic/social/my")
			   .WithResponse(new SocialList
			   {
				   PlayerId = MockBeamContext.DEFAULT_DBID,
				   friends = new List<Friend>(),
				   blocked = new List<global::Beamable.Common.Api.Social.Player>(),
				   invites = new List<FriendInvite>()
			   })
			   .WithToken(MockBeamContext.ACCESS_TOKEN);

			ctx.Requester.MockRequest<MultiplePlayersStatus>(Method.POST, "/presence/query")
			   .WithResponse(new MultiplePlayersStatus
			   {
				   playersStatus = new List<PlayerPresence>()
			   })
			   .WithToken(MockBeamContext.ACCESS_TOKEN);

			_mailSearchRoute = ctx.Requester
			   .MockRequest<MailSearchResponse>(Method.POST,
				   $"/object/mail/{MockBeamContext.DEFAULT_DBID}/search")
			   .WithRawResponse("{\"results\":[{\"content\":[],\"count\":0,\"name\":\"search\"}]}")
			   .WithToken(MockBeamContext.ACCESS_TOKEN);

			_sendFriendInviteRoute = ctx.Requester
			   .MockRequest<EmptyResponse>(Method.POST, "/basic/social/friends/invite")
			   .WithResponse(new EmptyResponse())
			   .WithToken(MockBeamContext.ACCESS_TOKEN);
		}

		[UnityTest]
		public IEnumerator SuppressedAutomaticMailUpdatesDoNotSearchMailWhenPlayerSocialStarts()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var social = Context.Social;
			yield return social.OnReady.ToYielder();

			Assert.AreEqual(0, _mailSearchRoute.CallCount);
		}

		[UnityTest]
		public IEnumerator DefaultPlayerSocialBehaviorSearchesMailWhenPlayerSocialStarts()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = false;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var social = Context.Social;
			yield return social.OnReady.ToYielder();

			Assert.AreEqual(1, _mailSearchRoute.CallCount);
		}

		[UnityTest]
		public IEnumerator SuppressedAutomaticMailUpdatesRejectSendingFriendInvites()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;
			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var social = Context.Social;
			yield return social.OnReady.ToYielder();

			Exception error = null;
			var promise = social.Invite(OtherPlayerId);
			promise.Error(ex => error = ex);
			yield return promise.ToYielder();

			Assert.IsInstanceOf<InvalidOperationException>(error);
		}

		[UnityTest]
		public IEnumerator SuppressedAutomaticMailUpdatesRejectAcceptingFriendInvites()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;
			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var social = Context.Social;
			yield return social.OnReady.ToYielder();

			Exception error = null;
			var promise = social.AcceptInviteFrom(OtherPlayerId);
			promise.Error(ex => error = ex);
			yield return promise.ToYielder();

			Assert.IsInstanceOf<InvalidOperationException>(error);
		}

		[UnityTest]
		public IEnumerator SuppressedMailChecksCanBeEnabledMidsession()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();

			var social = Context.Social;
			yield return social.OnReady.ToYielder();

			Assert.AreEqual(0, _mailSearchRoute.CallCount);

			yield return social.EnableFriendInvitationMailChecks().ToYielder();
			Assert.AreEqual(1, _mailSearchRoute.CallCount);
		}

		[UnityTest]
		public IEnumerator EnablingSuppressedMailChecksTwiceOnlySearchesMailOnce()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var social = Context.Social;
			yield return social.OnReady.ToYielder();

			yield return social.EnableFriendInvitationMailChecks().ToYielder();
			yield return social.EnableFriendInvitationMailChecks().ToYielder();

			Assert.AreEqual(1, _mailSearchRoute.CallCount);
		}

		[UnityTest]
		public IEnumerator EnabledMailChecksSearchMailWhenMailboxUpdateArrives()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var social = Context.Social;
			yield return social.OnReady.ToYielder();
			yield return social.EnableFriendInvitationMailChecks().ToYielder();

			Context.Api.NotificationService.Publish(MailUpdateChannel, null);
			var timeout = Time.realtimeSinceStartup + 1f;
			while (_mailSearchRoute.CallCount < 2 && Time.realtimeSinceStartup < timeout)
			{
				yield return null;
			}

			Assert.AreEqual(2, _mailSearchRoute.CallCount);
		}

		[UnityTest]
		public IEnumerator EnabledMailChecksAllowSendingFriendInvites()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var social = Context.Social;
			yield return social.OnReady.ToYielder();
			yield return social.EnableFriendInvitationMailChecks().ToYielder();

			yield return social.Invite(OtherPlayerId).ToYielder();

			Assert.AreEqual(1, _sendFriendInviteRoute.CallCount);
		}

		[UnityTest]
		public IEnumerator EnablingMailChecksDoesNotEnableAnotherPlayerSocialInstance()
		{
			CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial = true;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();
			var enabledSocial = Context.Social;
			yield return enabledSocial.OnReady.ToYielder();

			var suppressedSocial = new PlayerSocial(
				Context.ServiceProvider.GetService<ISocialApi>(),
				Context.ServiceProvider.GetService<INotificationService>(),
				Context.ServiceProvider.GetService<IMailApi>(),
				Context.ServiceProvider.GetService<IPresenceApi>());
			yield return suppressedSocial.OnReady.ToYielder();

			yield return enabledSocial.EnableFriendInvitationMailChecks().ToYielder();

			Exception error = null;
			var invitePromise = suppressedSocial.Invite(OtherPlayerId);
			invitePromise.Error(ex => error = ex);
			yield return invitePromise.ToYielder();
			yield return suppressedSocial.OnDispose().ToYielder();

			Assert.AreEqual(1, _mailSearchRoute.CallCount);
			Assert.IsInstanceOf<InvalidOperationException>(error);
			Assert.IsTrue(CoreConfiguration.Instance.SuppressAutomaticMailUpdatesFromPlayerSocial);
		}

	}

#if BEAMABLE_PURCHASING
	public class CommerceInitializationTests : BeamContextTest
	{
		private bool _originalSkipCommerceInitialization;
		private MockPlatformRoute<GetSKUsResponse> _getSkusRoute;

		[SetUp]
		public void SaveConfiguration()
		{
			_originalSkipCommerceInitialization = CoreConfiguration.Instance.SkipCommerceInitialization;
		}

		[TearDown]
		public void RestoreConfiguration()
		{
			CoreConfiguration.Instance.SkipCommerceInitialization = _originalSkipCommerceInitialization;
		}

		protected override void OnRegister(IDependencyBuilder builder)
		{
			base.OnRegister(builder);
			UnityBeamablePurchaserRegister.RegisterServices(builder);
			BeamablePurchaserConfigurationRegister.RegisterServices(builder);
		}

		protected override void OnInit(MockBeamContext ctx)
		{
			base.OnInit(ctx);
			_getSkusRoute = ctx.Requester.MockRequest<GetSKUsResponse>(Method.GET, "/basic/commerce/skus")
				.WithResponse(new GetSKUsResponse
				{
					skus = new SKUDefinitions
					{
						version = 1,
						created = string.Empty,
						definitions = new List<SKU>()
					}
				})
				.WithToken(MockBeamContext.ACCESS_TOKEN);
		}

		[UnityTest]
		public IEnumerator SkippedCommerceInitializationUsesDummyPurchaserWithoutRequestingSkus()
		{
			CoreConfiguration.Instance.SkipCommerceInitialization = true;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();

			Assert.AreEqual(0, _getSkusRoute.CallCount);
			Assert.IsInstanceOf<DummyPurchaser>(Context.Api.BeamableIAP.GetResult());
		}

#if !BEAMABLE_PURCHASING_IMPLEMENTATION_DISABLED
		[UnityTest]
		public IEnumerator DefaultCommerceInitializationUsesUnityPurchaserAndRequestsSkus()
		{
			CoreConfiguration.Instance.SkipCommerceInitialization = false;

			TriggerContextInit();
			yield return Context.OnReady.ToYielder();

			Assert.AreEqual(1, _getSkusRoute.CallCount);
			Assert.IsInstanceOf<UnityBeamablePurchaser>(Context.Api.BeamableIAP.GetResult());
		}
#endif
	}
#endif

}
