using Beamable;
using UnityEngine;

// TODO: remember about removing this one before final push/merge
public class TypeSafeChecks : MonoBehaviour
{
    // Start is called before the first frame update
    async void Start()
    {
	    var ctx = BeamContext.Default;
	    await ctx.OnReady;

	    // Currently we can't call AttachIdentity extension method because autogenerated microservice client doesn't
	    // implement ISupportsFederatedLogin interface yet
	    
	    // var client = ctx.Microservices().ExternalAuthMS();
	    // client.AttachIdentity<ThirdPartyIdentities.SolanaCloudIdentity>("x");
    }
}
