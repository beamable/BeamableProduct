---
version: '3'
services:
  cli:
    build:
      context: ../../../
      dockerfile: build/docker/cli/Dockerfile
    volumes:
      - ../../../:/Client
    environment:
      VERSION: ${VERSION:-0.0.0}
    