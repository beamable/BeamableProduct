---
version: '3'
services:
  microservice:
    build:
      context: ../../../
      dockerfile: build/docker/image.microservice/Dockerfile
    volumes:
      - ../../../:/Client
    environment:
      VERSION: ${VERSION:-0.0.0}