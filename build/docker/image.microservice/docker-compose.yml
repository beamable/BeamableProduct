---
version: '3'
services:
  microservice:
    build:
      context: ../../../
      dockerfile: build/docker/image.microservice/Dockerfile
    volumes:
      - ../../../:/Client
    environment:
      BUILD_TARGET_FRAMEWORK: ${BUILD_TARGET_FRAMEWORK}